# Go
/bin/
*.exe
*.out
*.test

# Node / frontend build
node_modules/
dist/
.vite/

# Secrets and env — never committed (secrets are delivered, not configured)
.env
.env.*
*.local
secrets/

# Python dev tooling — Doorstop runs from a local venv; its rendered output is
# regenerated on demand and never committed.
.venv/
docs/requirements/_published/

# Coverage / logs
coverage/
*.log

# OS / editor
.DS_Store
Thumbs.db
*.swp
