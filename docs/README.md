# Documentation index

Every fact about WiseKiosk has exactly one canonical home: a document that guarantees it. Every other
document may cite or summarize that fact, but never restates it as independent content. This table is
that referenceable definition, and [`CI.md`](CI.md)'s documentation-integrity gates hold it to that:
a citation resolves, and every tracked document is claimed by a row here unless it sits under a
top-level dot-directory, which holds machinery rather than documents. There is no exclusions list
([ADR 0014 rev 4](decisions/0014-documentation-index-claims-documents.md)); excluding anything else
takes an edit to the check.

The scope is facts *about WiseKiosk*. How a particular piece of code works is a fact about that code,
not about the product — it has no home in this table and belongs beside the code it explains.
An agent's tooling is a fact about whoever holds the tools, so **no document here names one**: not a
skill, vendored or global, and not the command that invokes it. A document that reaches for one is
describing its own author rather than the product, and states the obligation the tool carries instead.
Rationale is the part that may not live in a source artifact;
[`CONTRIBUTING.md`](../CONTRIBUTING.md)'s review checklist is where that is caught — whether a
comment states mechanism rather than reason, and whether a citation restates what it cites.

| Document | Guarantees | Excludes |
|---|---|---|
| [`../README.md`](../README.md) | The product definition and orientation: what WiseKiosk is, the module roster, who operates it and why that constraint outranks the others, the minimum specs, and the entry point to every other document. Cites the SYS items that make each claim normative | A testable "shall" obligation (the requirements tree); a decision's rejected alternative (an ADR); as-built structure (`ARCHITECTURE.md`) |
| [`ARCHITECTURE.md`](ARCHITECTURE.md) | The as-built structural description of the system, and the diagrams generated from the architecture model, which precede the code they draw | What a component must *do*, built or not (the requirements tree); decision rationale with a rejected alternative (an ADR); an element's own responsibility statement, which the prose here names a box without restating (the model source) |
| [`architecture/`](architecture/README.md) | The LikeC4 source model the `ARCHITECTURE.md` diagrams are generated from, and how its dev-only silo is laid out | The as-built prose description of the system (`ARCHITECTURE.md`); why LikeC4 rather than another notation (an ADR) |
| [`TESTING.md`](TESTING.md) | The test architecture — tiers, standing obligations, coverage stance, review cadence — written as a specification before tests exist, and the rationale for those choices | Individual test implementations; per-requirement traceability (the requirements tree); a test-strategy decision with a rejected alternative (an ADR) |
| [`CI.md`](CI.md) | **Every check on the repository**: what CI provides, what blocks a merge, what each gate is allowed to let through, and the reasoning for each stance. The canonical home for a repository-facing check — it constrains the repository, so no requirement states it | An obligation on the running software (the requirements tree); a check a machine cannot decide (`../CONTRIBUTING.md`'s review checklist); which tier a test belongs to (`TESTING.md`) |
| [`decisions/`](decisions/README.md) | A decision with a rejected alternative, carried as a versioned document: merged text is revisable, each correction is a rev, and every citation of one pins the rev it read | A decision with no real alternative considered (belongs in a commit message); an obligation on the running software (the requirements tree) |
| [`requirements/`](requirements/README.md) | **The specification.** Every testable "shall" obligation the system is built against; SYS→SRS→TST traceability; verification-reference gating (Doorstop); each item's own `rationale` for why that obligation exists | Product orientation and prose rationale *narrative* (the documents above); presentation or browsing (the docs site) |
| [`DEPLOYMENT.md`](DEPLOYMENT.md) | **What a deployment must do** to reach a running kiosk and to move between published digests: the obligations on the documented bring-up procedure, the defaults the shipped recipe carries, why the image declares a health signal and what reads it, and the position on configuration compatibility across an upgrade. The canonical home for an obligation on what ships beside the image rather than on the kiosk itself. What an operator receives is summarized here from the set `CI.md` states | An obligation on the running software (the requirements tree); the check that decides any of these, and the set a release publishes (`CI.md`); a decision with a rejected alternative (an ADR) |
| [`contracts/module-contract.md`](contracts/module-contract.md) | The canonical, self-contained statement of a contract an author follows by hand: what its parts are, how a module's need and its decomposition are authored, the module-side obligation on the architecture model, what adding one involves, and the shapes it does not fit — the six-part module contract | A testable "shall" obligation (the requirements tree); the per-module test obligations (`TESTING.md`); a decision with a rejected alternative (an ADR) |
| [`contracts/display-styling-contract.md`](contracts/display-styling-contract.md) | The display styling contract an author follows by hand: the region frame's usage, the grouping vocabulary, the type and spacing scales, the typeface, how shared tokens are delivered, and the two calibrated bounds the design is written against — the type-size floor and the emission ceiling | The configuration schema's region roster ([ADR 0025 rev 3](decisions/0025-display-region-roster.md)); a testable "shall" obligation (the requirements tree); the demonstrations, the calibration derivation, and the design viewing distance behind those bounds (`design/display-design-study.md`) |
| [`design/display-design-study.md`](design/display-design-study.md) | The visual design study behind the display's mirror and legibility properties: the in-situ demonstrations, the calibration derivation the two display bounds are chosen from, and the design viewing distance they were calibrated at. A study, not a specification — it cites the obligations it informs rather than restating them | A testable "shall" obligation (the requirements tree); the design language an author follows and the operative bound values (`contracts/display-styling-contract.md`); a decision's rejected alternative (an ADR) |
| [`../frontend/src/modules/clock/README.md`](../frontend/src/modules/clock/README.md) | The clock module's UI design spec: how the time and date compose on the display — the type step per element, the peer composition, the grouping, and the module's one look — with a reference render, each choice citing the requirement it realises | A testable "shall" obligation (the requirements tree); the shared design language (`contracts/display-styling-contract.md`); the component and its behaviour (the module source) |
| [`../frontend/src/modules/weather/README.md`](../frontend/src/modules/weather/README.md) | The weather module's UI design spec: how the present, hourly and daily readings compose on the display — the type steps, the toggling curve and card treatments, the grouping and states — with a reference render, each choice citing the requirement it realises | A testable "shall" obligation (the requirements tree); the shared design language (`contracts/display-styling-contract.md`); the component and its behaviour (the module source) |
| [`site/`](site/README.md) | A browsable, click-through presentation of the documents above, plus traceability views (needtables, matrices) | Being a source of truth — a generated view only, never original content |
| [`../CONTRIBUTING.md`](../CONTRIBUTING.md) | How a change gets made and merged, and **the design-first rule**: nothing is implemented that has not been written down first. The canonical home for that rule — it governs the process, so no requirement states it | What the system must do (the requirements tree); what WiseKiosk is (`../README.md`); working rules specific to an AI agent (`../CLAUDE.md`) |
| [`../scripts/`](../scripts/README.md) | The inputs the repository checks recorded there have been exercised against, in both directions — the defect each must catch, the legal input it must not reject, and the legal input it rejects anyway — and how to re-run a case, as one file per check under `cases/`. A check with no file has no record | What a check asserts, and why (`CI.md`); which tier a test belongs to (`TESTING.md`) |
| [`../SECURITY.md`](../SECURITY.md) | The threat model and how to report a vulnerability | Product or deployment truth (`../README.md`); the security obligations themselves (the requirements tree) |
| [`../CLAUDE.md`](../CLAUDE.md) | Working rules layered on top for an AI agent — review independence, and halt-and-ask where the specification is silent | Any fact about WiseKiosk (every document above); a testable obligation (the requirements tree) |
