active: true
derived: false
header: |
  No region is laid out within the mirror reserve
level: 38
links:
- SYS008: ouhMzhj5lp6pwr8iqxsRVAUoCmzwHZSZvY93pAx9r5Q=
normative: true
rationale: |
  The reserve survives assignment because nothing can be assigned into it. The page owns the region
  set and the configuration names a region per module, so a reserve the page declines to cover is one
  no position an operator can write addresses - which is the erosion this obligation exists against,
  and the whole of what it stops.
  It does not stop erosion by overflow. Modules stacking in a region adjacent to the reserve leave
  each less room, content exceeds the region, and
  SRS017<!-- Full-screen assembly at kiosk; reflow, no horizontal scroll, at narrower widths -->
  obliges that content to overflow - into the reserve, if that is where the region borders. Adding
  modules therefore does reach the reserve, by the route the overflow ruling permits rather than by
  the route this item closes. What is obliged here is the layout; what crosses it afterwards is the
  consequence of keeping content, which was chosen over hiding it.
  Stated over the regions the page lays out rather than over the surface that ends up empty. The
  emergent form was tried and withdrawn: a display covered but for its mandated edge inset leaves a
  thin band whose bounding box is the whole display, so any measure of the largest empty region's
  extent scores full marks on a display returning no reflection.
  The extent and placement are the check's, so calibrating how much of a person the display returns
  is not a specification change. They are not independent of the edge inset or the region set's own
  geometry, which is why one check reads them together.
  Three obligations spend the same finite height - this reserve, the inset in
  SRS034<!-- The laid-out regions keep clear of the display edge --> and the size floor in
  SRS033<!-- Text holds a minimum size against the display, at every resolution --> - and their
  figures are calibrated as a set against a display an operator would plausibly build. Past that
  density they cannot all hold, and that is not a conflict between them to be resolved by loosening
  one: an operator who loads a region past what it can show has overloaded it, and the result is
  theirs (owner, 2026-08-09). What the page does then is already ruled - the content overflows and
  nothing is rendered to announce it. No bound on the configuration follows, and none should be
  added; the figures move together or not at all.
ref: ''
reviewed: MSQ0t28_XaNvhzcPhLpxHJlYDLjTfTzO5y4rmGfW3m4=
status: accepted
text: |
  The display page shall lay out no region intersecting a contiguous area of the display of stated
  extent, placed to return the reflection of a viewer facing the display.
verification-justification: |
  Asserted by reading every laid-out region's geometry against the reserve area, over configurations
  chosen to load the display - the module roster at its fullest, and repeated instances of a module
  an operator may add - since the region set may itself depend on which regions are occupied. Four
  bounds. Content overflowing an adjacent region may cross into the reserve, which the overflow
  ruling permits, so the check settles that no region is placed there rather than that no pixel
  enters it. Whether the panel is hung so that a viewer stands before the reserve is the operator's,
  as mounting is. And that the reserve returns a recognisable reflection at all is a fact about
  half-silvered glass and the room, which no assertion over a rendered page reaches.
  The fourth is the one this item cannot escape by wording. The reserve's extent and placement are
  the check's, so the check supplies the area it then proves nothing was laid into - a reserve
  written as a corner of a few pixels passes while discharging nothing. What the assertion settles is
  that the region set avoids whatever area was declared; that the declared area is worth reserving is
  a human act, performed where the figures live and re-performed whenever they change.
verification-method: test
