active: true
derived: false
header: |
  A maintained table names each canonical document, its guarantee, and its exclusion
level: 1.75
links:
- SYS037: YBd-Vw7RN37YKfqQFzxBdH_we306Fr2alSZjJp40xi4=
normative: true
rationale: |
  Makes the taxonomy a standing artifact rather than an implicit convention. Placed at
  `docs/README.md` (new file): the taxonomy spans every canonical document (the repository README,
  FOUNDATIONS, ARCHITECTURE, TESTING, the ADR index, the requirements tree, the docs site,
  contracts), so it belongs at a
  repo-wide docs index rather than nested inside the Doorstop-specific `docs/requirements/README.md`.
ref: ''
reviewed: LGPm-wvWKXlNgpv0zEYy9fsst-Kxn9dWsdsE1jCP5N8=
status: accepted
text: |
  The repository shall maintain an explicit table naming each canonical document, what it guarantees,
  and what it explicitly excludes, so the single-home rule has a referenceable definition.
verification-method: inspection
