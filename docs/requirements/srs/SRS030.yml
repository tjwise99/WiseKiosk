active: true
derived: false
header: |
  Only content is rendered above the emission ceiling
level: 33
links:
- SYS008: tgO_Ccf2WuA2BKWSFdYoo4nI7qNYzHDOJs-4VhaNdOk=
normative: true
rationale: |
  The mirror is made of absence, so anything drawn where content is not is drawn at the mirror's
  expense: a panel behind a block of text, a card, a fill marking a region's bounds, a scrim lifting a
  glyph off its background. Each brightens the ground rather than darkening it, spending mirror on a
  legibility gain that does not arrive over a background already dark.
  Stated as a ceiling on emission rather than as a list of permitted devices. A list decides today's
  design - and a fourth device then becomes a specification change, which is what a requirement
  naming its own devices always costs. A ceiling states the property and leaves the devices to the
  design: a hairline divider and a grouping tint dark enough sit under it, a lit card or a scrim does
  not, and how dark is dark enough is calibrated against the glass in
  TST045<!-- Pending: emitting-surface test --> rather than fixed here. It also makes the obligation
  decidable, because reading a magnitude replaces sorting a device.
  Stated over emission rather than over the surface no content occupies, because occupancy is read
  from element boxes and a scrim would remove its own area from the population measured.
  The exemption is text and imagery, and it is the page's rather than a module's: a configuration
  that cannot be applied leaves no modules at all, and
  SRS004<!-- Page renders a legible error state for every configuration failure class --> obliges a
  legible error state anyway. Scoping it to a module's content would leave the page owing a message
  and forbidden to light it.
  What this permits is a large bright image rendered as content, which takes the mirror as surely as
  a scrim does. That is a module's design, and no item in this tree obliges otherwise.
ref: ''
reviewed: BCM7wrdzIj7VQIL7iUropw54iWTexBW44FjfLCiRh6Q=
status: accepted
text: |
  Only the text and imagery the display page presents as content shall be rendered above a stated
  fraction of the display's maximum emission.
verification-justification: |
  Asserted by reading every emitting surface of every element the page draws - ground, border,
  outline and shadow, the page's own root among them - and requiring each below the ceiling except
  where it is rendered text or imagery. The population is every element rather than a sampled area,
  so an element added to the page is an element the check reads, and it is every way an element emits
  rather than one of them. Reading a magnitude is what makes it decidable: whether a gradient behind
  a block is imagery or a scrim under another name is a judgement, whether it clears the ceiling is
  not.
  Four things sit outside the assertion. What the page renders is not what the panel emits -
  backlight, gamma, colour profile and any tone mapping lie between them, and a half-silvered surface
  attenuates the result again - so the check settles the value the software chose, not the light that
  leaves the glass. What a viewer sees in the unlit surface is the reflected room, which the software
  neither supplies nor controls. How much the exempt content itself emits is outside the assertion by
  construction: a large bright image satisfies this and takes the mirror, which the rationale records
  as unobliged rather than the check reporting green over it. And nothing bounds how much of the
  display sits at the ceiling, so a tint covering all of it passes.
verification-method: test
