active: true
derived: false
header: |
  A module that fetches nothing renders on through an outage
level: 43
links:
- SYS001: VGhozQiiHRgx8gA75WcFC3WXgp9KCc4dS_FDyA8tuGg=
normative: true
rationale: |
  A module that fetches has to stand down when the backend is gone, because a display cannot say the
  same outage once per region
  (SRS026<!-- The display says when the backend is gone -->). A module that fetches nothing shows
  nothing that is any less true for the backend being unreachable, so standing it down would take from
  a viewer something the outage never touched - degrading more of the display than the failure
  requires, which is what the parent forbids. That is why this sits beside the report rather than
  under it: one obliges the page to say the outage once, this obliges the outage to cost nothing it
  need not cost. Neither implies the other, and the display is only right when both hold.
  It is not a module's own unavailable state
  (SRS001<!-- A failed module shows why, and only that module -->), which is rendered for one cause
  and no other - the module's own source failing while the backend answered. A local module has no
  source that can fail that way.
  It is stated once, for every module it holds of, rather than per module. Nothing about it varies with
  which module is being rendered: a module that fetches nothing is undisturbed by an unreachable
  backend whatever it draws, so the sentence is the same sentence each time and a module carrying its
  own copy would be carrying this one twice.
ref: ''
reviewed: uHM-qJq6ZlHarVp6-gM0PhU8A1k9v1oI4ihgpxWfdJ0=
status: accepted
text: |
  While the backend is unreachable a module that fetches nothing shall go on rendering, neither
  standing down nor putting a state of its own in its place.
verification-justification: |
  Read from a rendered page whose backend is unreachable: the module's region still carries its
  content, still changing where its content changes, rather than being empty or carrying a message.
  Two things bound it. Unreachability is staged by the harness - one manner of failure among refusal,
  timeout, a wrong answer and a network that swallows the request - so this is evidence that a module
  taking nothing from the backend is undisturbed by one, rather than proof that no manner of outage
  can disturb it. And the obligation quantifies over every module that fetches nothing while the
  evidence reads the ones a suite exercises, so what is settled is the claim for those; a module that
  no check drives is unasserted here rather than covered by a sibling's reading.
  What the display says about the outage is a different item's, and this check asserts nothing about
  it.
verification-method: test
