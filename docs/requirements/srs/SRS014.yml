active: false
derived: false
header: |
  One validation implementation
level: 1.14
links:
- SYS014: null
normative: true
rationale: |
  A second engine enforces the schema slightly differently, and the divergence
  surfaces as a validator that accepts what boot rejects — the defect class
  the single-definition rule exists to kill (ADR 0007).
ref: ''
status: proposed
text: |
  Boot validation and the standalone validator shall execute the same
  validation implementation; the configuration schema's rules shall not be
  enforced by a second, parallel implementation.
verification-method: inspection
