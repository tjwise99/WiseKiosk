active: false
derived: false
header: |
  Pending: proxy endpoint & statelessness test
level: '1.20'
links:
- SRS018: null
normative: true
rationale: ''
ref: ''
reviewed: null
status: proposed
text: |
  Pending: backend integration test driving every endpoint the boundary contract
  declares for each supported source, asserting each returns its
  contract-shaped payload and that every source is reachable; and that the payload
  for a request is determined solely by that request — no per-client or session
  state — with cache-hit/miss behaviour (whether an upstream call occurs)
  covered separately by TST019. Lands with the backend skeleton (#9); activated
  and given a references entry then.
verification-justification: |
  Settles that every declared endpoint returns a contract-shaped payload and every source is
  reachable. Leaves unproven behaviour when an upstream returns a shape the contract does not
  describe.
verification-method: test
