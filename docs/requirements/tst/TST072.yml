active: false
derived: false
header: |
  Pending: repo-level dead-test detector
level: 1.5
links:
- SRS068: null
normative: true
rationale: ''
ref: ''
status: proposed
text: |
  Pending: enumerates committed test files (Go `_test.go`; frontend `*.test.*`/`*.spec.*`) and asserts
  each falls under a configured runner's reach; fails on a file excluded by skip, build tag, glob gap,
  or wrong directory. Lands with the first application test runners (go.mod / Vitest config);
  activated with a references entry then. The requirements tier is already covered by
  `doorstop --error-all` — do not re-encode it here.
verification-method: test
