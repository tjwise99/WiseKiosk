active: true
derived: false
header: |
  The module admits only a park in its supported set
level: 94
links:
- SRS055: zMlnMg1XZsVFLrEizVsaYIHuSO4vl171t6k_dVGXUN0=
normative: true
ref: ''
references:
- keyword: func TestTST072_ThePatternAdmitsASupportedParkAndRejectsEveryOther(t *testing.T)
    {
  path: backend/internal/modules/park_wait_times/park_wait_times_test.go
  sha: e9850ca303703a213159aeb673609bc8f90b97955554c1bb0f2353873f40c14a
  type: file
reviewed: qjN1j3iHjinhh9WE_YhHMz32T9KGAA8LhF5jTToBm7g=
status: accepted
text: |
  Unit test exercising validate against the module's own supported-park table, without a network:
  every slug the table declares is admitted and resolves the entity id declared for it, every other
  value is rejected, and a request naming one unsupported park among several supported ones is refused
  whole, with no upstream call issued for any of the parks it named.
verification-justification: |
  Settles that the declared constraint admits a park from the module's supported set and rejects the rest
  before any upstream call is made. Leaves unproven that an admitted park is one the source answers for on
  a given day (an upstream non-answer via the failure path), and whether the supported set still matches
  the source (the upstream-drift check's concern, not this one's).
verification-method: test
