active: false
derived: false
header: |
  Pending: workflows reference no credential; Contract tier is module-isolated
level: 1.19
links: []
normative: true
rationale: ''
ref: ''
reviewed: u1Tlu5q_sLaWBy8zgAHZmXaQhR-cZ6qevnM2wxXZD6E=
status: proposed
text: |
  Pending: test parsing every workflow under .github/workflows/ and asserting each secrets reference
  resolves to an allowlisted non-credential name, an allowlist rather than a denylist so an
  unanticipated credential fails closed; that no job invokes the Contract-tier target; and that the
  Contract tier is a nested module unreachable from the parent module's test discovery and from just
  verify. The checking script is this item's references target - ADR 0002 bars a Doorstop reference
  into a dot-directory, not a check that reads one.
verification-method: test
