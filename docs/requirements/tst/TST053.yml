active: true
derived: false
header: |
  Each hour form is the one its selection asked for
level: 75
links:
- SRS038: R0HOuBF7kotpzdPXK3DKBOVCnBN3NlGLiPsgG_-yau0=
normative: true
rationale: |
  Both selections are driven rather than one, because a module ignoring the configuration and hardcoding
  either form passes a check that reads only the other. The two renderings are also compared against
  each other at an hour that tells them apart - an afternoon hour, since morning hours read alike in
  both forms - so a module that varies the label without varying the hour is caught.
ref: ''
references:
- keyword: 'test(''TST053: presents the hour in the form its configuration selects'',
    async ({ page }) => {'
  path: frontend/src/modules/clock/clock.spec.ts
  sha: 940b2b5abc54a73b9cd4e57f9e8354e144ac0eafa838f186b55f4833bc114e28
  type: file
reviewed: TlD-a5R2RGZfzsS4fbxM1UymBY7O-lwaJRLfcRrME1s=
status: proposed
text: |
  Render test loading the page under each of the two hour selections at an afternoon host time,
  asserting that the hour presented is the one that selection calls for and that the two renderings
  differ from each other.
verification-justification: |
  Settles that the presented hour follows the selection under both values, which fails a module that
  hardcodes either form or that ignores the key.
  Leaves unproven everything about how the form is written - separator, zero-padding, and how a
  meridiem indicator reads - which the item does not oblige and no check judges.
verification-method: test
