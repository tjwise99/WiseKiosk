active: false
derived: false
header: |
  Pending: upstream-failure error payload test
level: 1.1
links:
- SRS001: LTQHmIb2r8J9C8oynE26q7VV0bBwnH11A-XGXuhWyH8=
normative: true
rationale: |
  Driven from the static route registration list rather than a hand-written route set, so coverage
  is total by construction and a route added later cannot escape the sweep -
  TST015's<!-- Pending: no credential material on the wire, over every registered route --> ground,
  against the same list. Malformed payload is the one class that is per module rather than
  transport: it is the shaping library failing to parse, so it is induced per route rather than
  once.
ref: ''
reviewed: i4afLwzutBs2u7-28uMyacj2U0s7CWeLxJKVRR-vKOE=
status: proposed
text: |
  Pending: backend integration test that, for every entry in the static route registration list,
  induces each failure class - unreachable, timeout, non-success status, malformed payload - and
  asserts each produces the structured error payload, with the four pairwise distinguishable from it,
  so a backend answering all four identically fails. Lands with the proxy skeleton; activated and
  given a references entry then.
verification-justification: |
  Settles that each failure class produces a payload of the declared shape and that the four are
  pairwise distinguishable from it. Leaves unproven that the four cover what real upstreams do - a
  source failing in a fifth way produces some payload and nothing here decides which.
verification-method: test
