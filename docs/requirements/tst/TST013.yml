active: false
derived: false
header: |
  Retired: exactly one validation implementation, by import graph
level: 1.14
links:
- SRS005: 9IX7EbtRmxgPsuiLHwJeJQFOuJn2u2oxcbAy-8Z2R-Q=
normative: true
rationale: |
  Retired (#129) with the requirement it verified, whose only other validation entry point was the
  desk validator. One engine imported by both call sites would still have enforced two rule sets if
  the callers passed different options, or a differently configured instance; the import-graph
  assertion could not see that, so it proved a shared dependency rather than a shared verdict.
ref: ''
reviewed: -kas5bkxaDVVl4iTQVsw1kI_37P4zrNNSxp_w36N78M=
status: proposed
text: |
  Retired, unwritten, with the requirement it verified. It would have run a fixture corpus - valid,
  unknown key, wrong type, missing required, and an error inside a module fragment - through both the
  page's validation entry point and the standalone desk validator, asserting identical verdicts and
  identical reported error sets for every fixture; and asserted that exactly one source file imports
  the schema-validation engine, that both paths resolve to that file in the import graph, and that no
  other file imports the engine directly. There is one validation entry point and nothing left to
  compare it against.
verification-justification: |
  Would have settled that both validation entry points return identical verdicts and error sets
  across the corpus, leaving unproven agreement outside it - five fixtures agreeing is evidence of
  one implementation rather than proof. Nothing is settled or left unproven by a retired item.
verification-method: test
