active: true
derived: false
header: |
  The weather comes from one source and no other
level: 78
links:
- SRS041: nOOTagO3XUWnLikYkiB4XHmUvvFMSSwQymfhVvStxxs=
normative: true
rationale: |
  Two things are asserted together because either alone passes a module that fails the item.
  A payload that looks like weather does not say which party produced it, so the check stages the source
  and reads the page against what that source alone returned. And a module reading its source can still
  be reading a second one beside it - blending, or falling back - so the traffic the module causes is
  watched as well.
ref: ''
references:
- keyword: 'test(''TST056: draws the weather its own route answered with, and reads
    no other source'', async ({'
  path: frontend/src/modules/weather/weather.spec.ts
  sha: f880f85ab7ba56af0672a1d1df48c539582de70df699f09f56406db0f2bd0148
  type: file
reviewed: KBTiAFboDG_l5GFmi0LUt0zFabvxecj4-OElP9fz9jE=
status: proposed
text: |
  Unit and render test driving the module against one staged weather source and asserting the weather
  rendered is that source's answer, with no part of it drawn from anywhere else, while observing the
  traffic the module's route causes and asserting no request reaches a second source.
verification-justification: |
  Settles that what a viewer reads is the one staged source's answer, over the traffic the harness
  observes.
  Leaves unproven what that source does behind its own boundary, it being another party's system, and
  bounds the absence half by what the harness watches - a destination it does not observe is not
  asserted about.
verification-method: test
