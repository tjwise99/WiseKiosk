active: true
derived: false
header: |
  The date appears and disappears with its selection
level: 76
links:
- SRS039: d3yZHqS_GP_wJlY6-tzUhABgyw1uMiBCaKut4Uo2YIc=
normative: true
rationale: |
  Omission is the half a check is fooled on. A module always drawing the date passes any assertion that
  reads only the selection that asks for it, so both selections are driven and the absent case is
  asserted as an absence in the clock's own region rather than anywhere on the page.
  The date read is checked against the host's, and across a move of the host clock to a second date
  rather than at one instant, because a placeholder or a build-time constant transcribed from that one
  instant is present too and passes a single read; moving the clock catches it and exercises the
  date's rollover in the one place that reads it.
ref: ''
references:
- keyword: 'test(''TST054: shows the date when its configuration asks and omits it
    when it does not'', async ({'
  path: frontend/src/modules/clock/clock.spec.ts
  sha: cf9bd873406b6860baa4f0932aac59990d6bb32b4b692c982bc88fa3f5fc9f25
  type: file
reviewed: 29sJ4TrjGgqr79UWIvvGmJEr2ZrD_Ax20ONL9ui6h9U=
status: proposed
text: |
  Render test loading the page under each of the two date selections against a held host clock,
  asserting that the selection asking for the date yields the host's current date beside the time, and
  that the selection not asking for it leaves the clock's region carrying a time and no date.
verification-justification: |
  Settles that the date's presence follows the selection in both directions, and that what is shown
  when it is present is the host's date rather than a fixed string.
  Leaves unproven that the host's date is the true date, the host being outside this system, and says
  nothing about how the date is written or where in the region it sits.
verification-method: test
