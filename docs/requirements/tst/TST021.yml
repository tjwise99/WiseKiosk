active: false
derived: false
header: |
  Pending: the registered middleware chain is exactly the declared one
level: 1.17
links:
- SRS023: RkybxYpXWXU4lnHw3cOZEkY3_9dxMGfAQCQ4do6wXjM=
normative: true
rationale: |
  An equality against the declared chain, not a search for session or authentication packages: a
  denylist of known middleware passes the first time one is hand-rolled, and this repository rejects
  denylist controls on principle. Equality fails closed — any middleware nobody declared, whatever
  it is called, breaks the assertion.
ref: ''
reviewed: 9yssln6Z2gkSm-iUoY3dnky82o_JgcY2_PmnE6JoWXo=
status: proposed
text: |
  Pending: check that the middleware chain registered on the constructed router equals the declared
  chain exactly, so no session, authentication, or access-control middleware can be introduced on
  the request path without the declaration changing. Runs once the backend skeleton (#9) exists.
verification-justification: |
  Settles that the registered middleware chain equals the declared one. Leaves unproven identity
  established inside a handler rather than in middleware.
verification-method: test
