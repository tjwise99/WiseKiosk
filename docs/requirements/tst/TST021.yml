active: false
derived: false
header: |
  Pending: every external source has a proxy route
level: 1.21
links:
- SRS021: null
normative: true
rationale: ''
ref: ''
status: proposed
text: |
  Pending: test asserting the set of external sources declared by upstream-backed modules equals the
  set of registered GET /api/<source> routes, in both directions, so no source is exempted from the
  proxy and no route exists without a source; and that no frontend or backend code constructs an
  upstream URL outside a module's shaping library. Lands with the backend skeleton (#9).
verification-method: test
