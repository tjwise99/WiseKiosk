active: false
derived: false
header: |
  Pending: shaping purity and secret-free signatures
level: 1.33
links: []
normative: true
rationale: ''
ref: ''
reviewed: uX4W2y7BDGOJMykznsh6uHoVJ-C3MrJcVGrbMft8X_k=
status: proposed
text: |
  Pending: test asserting each module's shaping package resolves a transitive import set that is a
  subset of a declared pure-package allowlist, so I/O is absent by construction rather than by a
  denylist of forbidden packages; asserting no exported shaping function's parameters include the
  secret type or the URL-builder's output type; and running the shaping unit tests with a transport
  that panics on use. Lands with the first module and the repo layout (#5).
verification-method: test
