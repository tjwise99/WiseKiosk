active: false
derived: false
header: |
  Pending: Svelte/TypeScript static security scan gate test
level: 1.45
links:
- SRS058: null
normative: true
rationale: ''
ref: ''
status: proposed
text: |
  Pending: check that materialises the seeded vulnerable Svelte/TypeScript fixture into a
  temporary tree (ADR 0010), runs the same code-scanning analysis the production job runs, and
  asserts the expected finding is reported (expected: CodeQL javascript-typescript). Fails if the
  analysis is absent, skips the frontend tree, or lets the seeded finding pass. Lands with the
  first frontend code; activated with a references entry to the fixture and the meta-gate then.
verification-method: test
