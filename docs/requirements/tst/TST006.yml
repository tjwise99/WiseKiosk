active: true
derived: false
header: |
  Bind-mounted config demonstration
level: 1.9
links:
- SRS018: jAdiVAOaHeEAP9caGAN9vBMpjclpje_k3TmKnf5abBw=
normative: true
rationale: |
  Asserts what the mount delivers, not what the image omits: that nothing sits at the configuration
  path in the image is
  TST009's<!-- The published image carries no deployment specifics -->, under the same
  parent.
ref: ''
references:
- path: scripts/image/config_mount.py
  sha: e8971b80f23ecdda2c8d90b88a9c553f419d5437f4c51414cff885f14b55122b
  type: file
reviewed: LZEvuf2xECnEdSfMz_NdjDNygFgAivvLAVrPKtieF5U=
status: accepted
text: |
  Run the published image with a fixture configuration bind-mounted at the configuration path,
  asserting the served configuration reflects the fixture byte-for-byte; and run the same image with
  no mount, asserting it fails loudly rather than serving a baked default.
verification-justification: |
  Settles byte-fidelity of the mounted configuration, and loud failure rather than a baked default
  when no mount is supplied. Leaves unproven behaviour when the mount exists but is unreadable.
verification-method: test
