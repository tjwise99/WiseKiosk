active: true
derived: false
header: |
  The weather module's location constraint admits and rejects
level: 80
links:
- SRS043: rFg2Wzv4P8LHpir69U-rSIpHFSkzgA2K72zWWFD8s5E=
normative: true
rationale: |
  The rejecting half is where a check is fooled. A module admitting everything passes a test fed only
  good input, so the non-conforming cases carry the assertion and the admitting ones only establish that
  the constraint is not refusing its own subject.
  The non-conforming cases are values rather than spellings. A body that is not a number at all never
  reaches the constraint, being refused by the decoding, so what this exercises is the half the module
  owns - a well-formed number outside the earth's surface - and the malformed-spelling cases the
  earlier query shape needed are no longer this item's to enumerate.
  The no-request clause is read with them rather than separately: a rejection that still went upstream
  is exactly the failure the item forbids, and it is invisible in the status a caller gets back.
ref: ''
references:
- keyword: func TestTST058_ThePatternAdmitsAPointAndRejectsEveryOtherValue
  path: backend/internal/modules/weather/weather_test.go
  sha: ffa361137d24db1f1d5f1f14b3b3c043d661dd2562ce4efc894d2f4514068fe5
  type: file
reviewed: h9vEGl6wiGIVcZceEcBAylB9b6H9c7uwsXZ7MbUENEs=
status: proposed
text: |
  Unit test exercising the constraint the module declares over conforming and non-conforming
  locations, asserting that a point on the earth's surface is admitted, that every other value is
  rejected, and that no upstream request is issued for a rejected one.
verification-justification: |
  Settles which values the module admits, and that a rejected request costs no upstream call.
  Leaves unproven that an admitted location is one the source can answer for - a well-formed point the
  source has nothing for is an upstream failure rather than a rejection - and bounds the rejecting half
  by the non-conforming cases the suite enumerates, a malformation nobody thought of being unasserted.
verification-method: test
