active: false
derived: false
header: |
  Pending: Go static security scan gate test
level: 1.44
links:
- SRS058: null
normative: true
rationale: ''
ref: ''
status: proposed
text: |
  Pending: check that materialises the seeded vulnerable Go fixture into a temporary tree
  (ADR 0010), runs the same code-scanning analysis the production job runs, and asserts the
  expected finding is reported at or above the configured severity threshold. Fails if the
  analysis is absent, skips the Go tree, or lets the seeded finding pass. Lands with the first
  backend code; activated with a references entry to the fixture and the meta-gate then.
verification-method: test
