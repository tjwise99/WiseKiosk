active: false
derived: false
header: |
  Pending: configuration failure-class render test
level: 1.13
links:
- SRS004: O23b_MA8QsS8IFJgOBlC9nT_lFUCm1xIHb3VreqwrgA=
normative: true
rationale: |
  Scoped to naming the failure. What the state must say beyond that is TST005
  <!-- Pending: a module-scoped configuration error lands at that module -->'s, which asserts it on
  the branch where a correction exists to state.
ref: ''
reviewed: pm64VqJhA0S9HSc0MFKhRTLNnYPAnydHvCLNk7ne-gs=
status: proposed
text: |
  Pending: render test asserting the page shell loads and renders an operator-language error state
  naming which configuration failure occurred, for each failure class - absent, unfetchable,
  unparseable, and schema-invalid. Lands with the frontend skeleton
  (#10); activated and given a references entry then.
verification-justification: |
  Settles that each named failure class renders a state that identifies it, without a valid
  configuration. Leaves unproven that the four classes are exhaustive.
verification-method: test
