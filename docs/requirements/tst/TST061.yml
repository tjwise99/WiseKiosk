active: true
derived: false
header: |
  The weather shown follows its source inside the freshness bound
level: 83
links:
- SRS046: twOwP_ffa_ll4QDgilsFX0QBaqRfP1KwM6EUN9qcKwA=
normative: true
rationale: |
  Two things are staged in the render check because neither alone makes the bound decidable. A source
  that never changes its answer cannot show the page following one, and a wall clock cannot be run for
  the length of the bound inside a suite, so the source is moved and the clock is driven.
  The reload is what the assertion is against. A page that picked up the change only when it was loaded
  again would satisfy any reading taken after a reload while leaving a display that is never reloaded
  showing weather from whenever it started.
ref: ''
references:
- keyword: 'test(''TST061: follows its source to a new reading inside the freshness
    bound, without reloading'', async ({'
  path: frontend/src/modules/weather/weather.spec.ts
  sha: 228a31229b91f1a4f8a3e09cd2d253af9e17e75ca1f2c71fbfb519d279b536ae
  type: file
- keyword: func TestTST061_TheCacheIntervalHoldsAnAnswerNoLongerThanTheFreshnessBound
  path: backend/internal/modules/weather/weather_test.go
  sha: f5ba25f4a05d0ad930be295deff56e0eca35b49538251787368a84db9a22f6ae
  type: file
reviewed: E9E0KSdTg1pEJfu4QoJ3UAqIksyhRWvaNCV3d-Al7Vc=
status: proposed
text: |
  Two checks, one per half of what the bound is met between. A unit test over the module's registered
  policy, asserting the interval a served answer is held for is no longer than the freshness bound the
  requirement states. And a render test holding the page open against a source staged to change its
  answer and a driven clock, asserting the region carries the changed answer on the display's own
  cadence and without the page being reloaded.
verification-justification: |
  Settles both halves: that an answer is held no longer than the figure the requirement states, and
  that what is drawn follows a change at the source on a page nobody reloaded. Leaves unproven how far
  behind the weather the source itself runs, which is another party's and outside this system. The
  render half is asserted against a clock the harness drives rather than a wall clock, so what is
  settled there is the module's behaviour as time is presented to it, and a page left running far
  longer than the suite runs is outside it.
verification-method: test
