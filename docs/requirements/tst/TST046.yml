active: true
derived: false
header: |
  Region overflow render test
level: 72
links:
- SRS031: ph2eZ6zSvcd5yzfKWArF7gSsJNmd8n90iVM6pzpqcr8=
normative: true
rationale: ''
ref: ''
references:
- keyword: test('content larger than its region exceeds the region rather than fitting
    inside it'
  path: frontend/tests/render/region-overflow.spec.ts
  sha: b01d9019b4d968e2cf861be28904c3d8895bf76cfc1ac51ad5e189a4cf729647
  type: file
- keyword: test('content that fits its region is not reported as overflowing'
  path: frontend/tests/render/region-overflow.spec.ts
  sha: b01d9019b4d968e2cf861be28904c3d8895bf76cfc1ac51ad5e189a4cf729647
  type: file
- keyword: test('the overflow is neither clipped nor scrolled away nor scaled down'
  path: frontend/tests/render/region-overflow.spec.ts
  sha: b01d9019b4d968e2cf861be28904c3d8895bf76cfc1ac51ad5e189a4cf729647
  type: file
reviewed: 9L5hOU2DzlU6Ehrzjg7yPy71Nq5Fm-N1f8bet1ZeGhs=
status: accepted
text: |
  Render a fixture whose content exceeds the region it is given, at the kiosk viewport (1920x1080)
  and at the desktop viewports (1280x800 and 1440x900), asserting at each that the content is
  neither clipped nor reachable only by scrolling nor scaled below the size it renders at when it
  fits, and that its box exceeds the region's. Seeded both ways: content that overflows, which
  must be reported as overflowing, and content that fits its region, which must not.
verification-justification: |
  Settles that content too large for its region overflows rather than being hidden, at three
  viewports. Its fixture is separate from the layout check's deliberately: overflowing content may
  cross a neighbouring region and may extend the document past the viewport, which the requirement
  permits, so one fixture asserting both would fail on a page behaving exactly as obliged. What that
  costs is the colliding case itself - the layout is shown disjoint and unscrolled, and content is
  shown kept, but never the two together, because the requirement says they need not hold together.
verification-method: test
