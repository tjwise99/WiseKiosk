{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "f=\"${CLAUDE_PROJECT_DIR:-.}/CONTRIBUTING.md\"; m=\"The SessionStart hook in .claude/settings.json could not deliver CONTRIBUTING.md from $f. Read it before changing anything in this repository.\"; n=\"CONTRIBUTING.md for this repository, injected at session start, up to but excluding the review checklist. That is a review artifact and reaches you at turn end, scoped to what you actually changed, from .claude/hooks/review-diff.py.\\n\\n\"; out=$(sed '/^## Review checklist/,$d' \"$f\" 2>/dev/null | jq -Rs --arg m \"$m\" --arg note \"$n\" 'if test(\"\\\\S\") then {hookSpecificOutput:{hookEventName:\"SessionStart\",additionalContext:($note + .)}} else {hookSpecificOutput:{hookEventName:\"SessionStart\",additionalContext:$m}} end' 2>/dev/null) && printf '%s' \"$out\" || printf '%s\\n' \"$m\"",
            "statusMessage": "Loading CONTRIBUTING.md"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PROJECT_DIR}/.claude/hooks/review-diff.py\""
          }
        ]
      }
    ]
  }
}
