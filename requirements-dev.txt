# Dev-only tooling. No application Python ships in WiseKiosk — this pins the
# requirements-management tool (Doorstop) used by the `check-reqs` gate. Install
# into a local venv, never globally:
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements-dev.txt
# CI installs the same pin (see .github/workflows/checks.yml, job `requirements`).
doorstop==3.2
