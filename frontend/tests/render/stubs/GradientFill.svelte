<!-- Seed: a region fill spelled as a gradient rather than a solid, above the ceiling. -->
<div class="stub" data-stub="gradient-fill">
  <p class="line">Above the ceiling</p>
</div>

<style>
  .stub {
    padding: var(--space-sm);
    background-image: linear-gradient(#777, #222);
  }

  .line {
    margin: 0;
    font-size: var(--type-body);
    font-weight: var(--type-body-weight);
  }
</style>
