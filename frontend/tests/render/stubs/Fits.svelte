<!-- A module whose content fits any region it is given. -->
<div class="stub" data-stub="fits">
  <p class="line">Fits</p>
</div>

<style>
  .stub {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .line {
    margin: 0;
    font-size: var(--type-body);
    font-weight: var(--type-body-weight);
  }
</style>
