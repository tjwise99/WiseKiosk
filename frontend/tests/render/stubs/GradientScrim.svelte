<!-- Seed: a scrim spelled as a gradient rather than a solid, above the ceiling. -->
<div class="stub" data-stub="gradient-scrim">
  <p class="line">Above the ceiling</p>
</div>

<style>
  .stub {
    padding: var(--space-sm);
    background-image: radial-gradient(rgb(170 170 170), rgb(0 0 0 / 0%));
  }

  .line {
    margin: 0;
    font-size: var(--type-body);
    font-weight: var(--type-body-weight);
  }
</style>
